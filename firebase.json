{
  "functions": [
    {
      "source": "functions",
      "codebase": "default",
      "ignore": [
        "node_modules",
        ".git",
        "firebase-debug.log",
        "firebase-debug.*.log",
        "*.local"
      ],
      "predeploy": [
        "npm.cmd --prefix \"%RESOURCE_DIR%\" run lint",
        "npm.cmd --prefix \"%RESOURCE_DIR%\" run build"
      ]
    }
  ],
  "firestore": {
    "rules": "firestore.rules"
  }
}
